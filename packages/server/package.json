{
  "name": "@go-electrify/server",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@go-electrify/shared": "workspace:*",
    "socket.io": "^4"
  },
  "devDependencies": {
    "ts-node-dev": "^2",
    "typescript": "^5",
    "@types/node": "^20"
  }
}
